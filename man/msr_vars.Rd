% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_vars.R
\name{msr_vars}
\alias{msr_vars}
\alias{kernel_var}
\alias{landscape_var}
\title{Define multiScaleR covariate transformations}
\usage{
msr_vars(...)

kernel_var(source)

landscape_var(source, metric, radius = NULL, base = exp(1), classes_max = NULL)
}
\arguments{
\item{...}{Named `kernel_var()` or `landscape_var()` specifications.}

\item{source}{Character. Name of the source raster layer.}

\item{metric}{Character. Landscape metric to calculate for `landscape_var()`.}

\item{radius}{Optional fixed buffer radius. If omitted, the radius is optimized.}

\item{base}{Logarithm base for diversity metrics.}

\item{classes_max}{Optional maximum number of classes for relative patch
richness (`rpr`).}
}
\value{
`msr_vars()` returns a data frame of class `"multiScaleR_vars"` describing
the requested covariate transformations.
}
\description{
These helpers define named model covariates as transformations of one or more
source raster layers. They are optional; if omitted, `kernel_prep()` preserves
the historical behavior where each raster layer becomes one optimized
kernel-weighted covariate with the same name as the raster layer.
}
\examples{
vars <- msr_vars(
  forest_prop = kernel_var("forest"),
  forest_ed = landscape_var("forest", metric = "ed"),
  cover_shdi_500 = landscape_var("landcover", metric = "shdi", radius = 500)
)

}
